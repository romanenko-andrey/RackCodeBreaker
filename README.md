Rack codebreaker

add to Gemfile:
    gem 'rack'
    gem 'colorize'
    gem 'rav_codebreaker'


run in console: rakeup

run in c9: rails s -p $PORT -b $IP
https://rack-codebreaker-aromanenko.c9users.io/

